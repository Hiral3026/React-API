extends layout 


block main-content 
  -//You can overwrite the main-content block by writing block content
  -//in the template file which is extending.
  h1.page-title Hello! Here's is the list of recommended movies! 
  ul 
    each movie in movieList 
      li
        a(href=`/ratings/${movie.movie.ids.tmdb}`) #{movie.movie.title} (#{movie.movie.year})
  
  